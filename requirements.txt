mcp[cli]>=1.2.0
httpx>=0.27.0
python-dotenv>=1.0.0
playwright>=1.40.0

# Used by scripts/import_cookies_from_chrome.py (the recommended way to
# bootstrap the Etsy browser session). Skip this dep if you'll only use
# scripts/bootstrap_browser_login.py instead.
browser-cookie3>=0.19.0
